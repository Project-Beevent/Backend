server:
  port: 8080
  error:
    include-message: always
spring:
  cors:
    allowed-origins:
      - "*"

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  config:
    location: java/itu/blg411/kanver:application-test.yaml



